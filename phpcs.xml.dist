<?xml version="1.0"?>
<ruleset name="TWITCHAPI-PSR12">
  <!-- Usamos el estándar PSR-12 -->
  <rule ref="PSR12"/>

  <!-- Analiza sólo el código de src/ -->
  <file>src</file>

  <!-- Si tuvieras tests, podrías excluirlos así:
  <exclude-pattern>tests</exclude-pattern>
  -->
</ruleset>

